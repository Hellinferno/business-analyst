Review the pull request or code changes provided.

1. Run `gh pr list` to show open PRs if no PR number is given
2. Run `gh pr view $ARGUMENTS` and `gh pr diff $ARGUMENTS` to get details
3. Provide a thorough review covering:
   - Overview of changes
   - Code quality and style
   - Potential bugs or risks
   - Security considerations
   - Test coverage
